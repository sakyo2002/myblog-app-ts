import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ReactIcon() {
  return (
    <Typography variant="h6" sx={{ fontSize: '25px', color: '#black' }}>
      Blog
    </Typography>
  );
};

